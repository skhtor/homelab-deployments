---
ingress:
  enabled: false

route:
  main:
    enabled: true
    hostnames:
      - requests.sass.sh
    parentRefs:
      - name: public
        namespace: networking
        sectionName: https

config:
  persistence:
    size: 500Mi
    storageClass: longhorn
