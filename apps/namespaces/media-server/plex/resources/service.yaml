apiVersion: v1
kind: Service
metadata:
  name: plex
  namespace: media-server
spec:
  externalTrafficPolicy: Local
  loadBalancerIP: 192.168.20.194
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: http
  selector:
    app: plex
  type: LoadBalancer
