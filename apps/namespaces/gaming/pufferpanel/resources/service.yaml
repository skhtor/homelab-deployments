apiVersion: v1
kind: Service
metadata:
  name: pufferpanel-web
  namespace: gaming
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: http
  selector:
    app: pufferpanel
  type: ClusterIP
